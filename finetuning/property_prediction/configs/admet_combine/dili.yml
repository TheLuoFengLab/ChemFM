{
  dataset_group: "ADMET",
  dataset: "dili",
  task_type: "classification",
  num_tasks: 1,
  scaler: False,
  log_scaler: False,
  weight_loss: True,
  metric: "auroc",
  pretrain_model: "ChemFM/ChemFM-3B",
  tokenizer_path: "tokenizer",
  optim: "adamw_torch",
  weight_decay: 0.01,
  lr_scheduler_type: "cosine_with_min_lr",
  learning_rate: 4.0e-4,
  min_learning_rate: 4.0e-5,
  num_train_epochs: 10,
  early_stopping_patience: 10,
  early_stopping_start_epoch: 100,
  warmup_ratio: 0.05,
  attention_dropout: 0.3,
  lora: True,
  lora_rank: 8,
  lora_alpha_ratio: 1.0,
  lora_dropout: 0.3,
  per_device_train_batch_size: 8,
  gradient_accumulation_steps: 2,
  molecule_source_aug_prob: 1.0,
  num_data_seeds: 1,
  seed: 533,
  output_dir: "./outputs/admet_combine/dili",
  adapter_name: "admet_dili",
  combine_train_val_test: True
}